import 'package:flutter/material.dart';

const mytheme = Color.fromRGBO(10, 142, 217, 1);
const lightGrey = Color.fromRGBO(243, 243, 243, 1);

const myGradient = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [Color.fromRGBO(131, 203, 244, 1), Color.fromRGBO(48, 161, 225, 1)],
);

const myGradient2 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [Color.fromARGB(28, 0, 0, 0), Color.fromARGB(151, 0, 0, 0)],
);

const myGradient3 = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [Color.fromARGB(66, 255, 255, 255), Colors.white],
);